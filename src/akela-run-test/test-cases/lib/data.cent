use lib::base::*;

Data_type_list {
    Data_type {
        .type = Type::Int8
        .align = 1
    }
    Data_type {
        .type = Type::Int16
        .align = 2
    }
    Data_type {
        .type = Type::Int32
        .align = 4
    }
    Data_type {
        .type = Type::Int64
        .align = 8
    }
    Data_type {
        .type = Type::Nat8
        .align = 1
    }
    Data_type {
        .type = Type::Nat16
        .align = 2
    }
    Data_type {
        .type = Type::Nat32
        .align = 4
    }
    Data_type {
        .type = Type::Nat64
        .align = 8
    }
    Data_type {
        .type = Type::Real16
        .align = 2
    }
    Data_type {
        .type = Type::Real32
        .align = 4
    }
    Data_type {
        .type = Type::Real64
        .align = 8
    }
}